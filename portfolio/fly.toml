app = "portfolioo-141"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[deploy]


[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']
